<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.caredog">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="caredog"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Caredog">

        <activity android:name=".FirstActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity"></activity>

        <activity android:name=".SignUp2_Dog_Activity"></activity>
        <activity android:name=".SignUp1_User_Activity"></activity>

        <activity android:name=".MainActivity"></activity>

        <activity android:name=".dataActivity"></activity>

        <activity android:name=".settingActivity"></activity>

        <activity android:name=".GooglemapActivity"></activity>

        <!--구글API key-->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDLtCoCRY-hNpnqlK7rEcmLuYyS71xwiI4" />
    </application>

</manifest>